const num = ["1", "2", "3"];
let numLen = num.length;

for(let i = num.length - 1; i >= 0; i--) {
    reversedArray.push(num [i]); // Aggiunge gli elementi in ordine inverso
}

console.log("Array invertito:", reversedArray);